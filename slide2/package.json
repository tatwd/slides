{
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build --base /slide2 --out ../dist/slide2",
    "export": "slidev export --with-clicks"
  },
  "name": "yarp-intro-slide",
  "description": "[WIP] YARP 初探",
  "version": "0.1.0"
}
