{
  "name": "slides",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "slide*"
  ],
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build && cp ./list.html ./dist/index.html",
    "serve": "serve -c ./vercel.json ./dist && cp ./list.html ./dist/index.html"
  },
  "devDependencies": {
    "playwright-chromium": "^1.14.1",
    "serve": "^12.0.1"
  }
}