{
  "name": "slides",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "slide2",
    "slide3"
  ],
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build && cp ./list.html ./dist/index.html",
    "serve": "cp ./list.html ./dist/index.html && serve -c ./vercel.json ./dist"
  },
  "devDependencies": {
    "playwright-chromium": "^1.14.1",
    "serve": "^12.0.1"
  }
}
