{
  "name": "slides",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "slide*"
  ],
  "private": true,
  "scripts": {
    "build": "cp ./list.html ./dist/index.html && yarn workspaces run build",
    "serve": "cp ./list.html ./dist/index.html && serve -c ./vercel.json ./dist"
  },
  "devDependencies": {
    "playwright-chromium": "^1.14.1",
    "serve": "^12.0.1"
  }
}